# GENUT_service runtime dependencies
# (kept in sync with pyproject.toml [project.dependencies];
#  `pip install -e .` is the canonical install)

fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
typer>=0.12

# Uncomment to use PostgreSQL (default DB is SQLite):
# psycopg[binary]>=3.1
